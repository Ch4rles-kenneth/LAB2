/**
 * /camera route — Phase 2: Camera Fundamentals
 *
 * This is a thin Expo Router route file. All camera logic lives in
 * src/screens/CameraScreen.tsx so it stays reusable and testable
 * independently of the routing layer.
 */
import CameraScreen from '@/screens/CameraScreen';

export default CameraScreen;
